# 옷장비서 v5 업데이트

## 🔒 1. 데이터 안 사라지게 (가장 중요!)

### 이중 저장 시스템
- 기존: localStorage 하나만 사용 (브라우저가 청소하면 날아감)
- 신규: **localStorage + IndexedDB 둘 다에 저장**
- 한쪽이 사라져도 다른 쪽에서 자동 복구
- 앱 켜면 양쪽 비교해서 더 많은 데이터 쪽으로 자동 복구

### 영구 저장 권한 요청
- `navigator.storage.persist()` 호출
- 브라우저가 데이터를 함부로 못 지우게 보호
- (모바일 사파리는 이 기능 제한적이라 백업이 더 중요)

### 백업 리마인더
- 데이터 5건 이상 + 7일 이상 백업 안 하면 홈 화면 상단에 알림
- 한 번 누르면 JSON 백업 파일 다운로드

---

## ⭐ 2. 별점 만족도

### 옷 등록 시
- 1~5점 별점 입력 가능 (선택사항)
- 옷장에서 별점 옷에 ★숫자 오버레이로 표시
- 좋아하는 옷 한눈에 파악 가능

### 착샷 등록 시
- 오늘 코디 만족도 별점
- 갤러리에 ★숫자 표시

---

## 📸 3. 착샷 → 옷장 빠른 등록

기존: 착샷 1장 = 코디 사진 한 장만 저장
신규: 착샷 저장할 때 **이름만 적으면 상의/하의 자동으로 옷장에 등록**

### 흐름
1. 착샷 모달 열기
2. 사진 추가 + 메모 + 별점
3. **"이 코디에 입은 옷을 옷장에도 등록" 패널**:
   - 상의: 카테고리 선택 + 이름 입력
   - 하의: 카테고리 선택 + 이름 입력
4. 저장 누르면 → 착샷 1개 + 옷 0~2벌 한번에 저장됨
5. 같은 사진을 옷장 카드에 자동으로 씀

### 중복 검사
입력하는 동안 비슷한 이름의 옷이 있으면 경고 표시

---

## 🔍 4. 중복 옷 검사

### 옷 등록 / 빠른 등록 / 착샷 등록
모든 등록 모달에서 이름 입력하는 동안:
- 비슷한 이름의 옷이 있으면 노란 경고박스
- 비슷한 옷 최대 3개 표시
- 옆에 **삭제** 버튼 → 한 번에 정리 가능

---

## 📋 데이터 구조 변경

`clothes` 객체에 새 필드 추가:
- `rating`: 0~5 (만족도)
- `fromOutfit`: 착샷에서 자동 등록된 경우 해당 착샷 ID

`outfits` 객체에 새 필드:
- `rating`: 0~5 (코디 만족도)

`settings` 객체에 새 필드:
- `lastBackupAt`: 마지막 백업 시간

기존 데이터는 그대로 호환 (rating 없으면 0으로 처리)

---

## 🚨 데이터 사라짐 사고 대처

만약 데이터가 또 사라진 것 같으면:

### 1. 브라우저 데이터 확인
크롬 → F12 (개발자 도구) → Application 탭 → Storage:
- **Local Storage** → `closet-helper-v3` 키 확인
- **IndexedDB** → `otjang-secretary` → `app-data` 확인

### 2. 같은 URL인지 확인
- 어제 접속한 주소와 오늘 주소가 정확히 같은지
- `m.tistory.com` vs `tistory.com` 같은 차이도 데이터 분리됨

### 3. 같은 브라우저인지 확인
- 사파리 ↔ 크롬 / 데스크탑 ↔ 모바일 사이는 데이터 공유 안 됨
- 시크릿 모드는 매번 초기화

### 4. 백업 복원
- 백업 JSON 파일 있으면 → 더보기 → 백업 복원

---

## 📝 사용 팁

- **별점은 옷장 정리에 좋아요**: 1점짜리 옷이 많다면 처분 후보
- **착샷에 옷 이름 등록**: 옷장 채우기가 훨씬 빨라짐
- **백업은 주 1회**: 데이터 안전하게
- **사파리 사용 시 주의**: iOS Safari는 가끔 자동 정리. 백업 더 자주
